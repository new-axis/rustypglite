[workspace]
members = ["rustypglite-sys", "rustypglite"]
resolver = "2"

[workspace.package]
version = "0.1.1"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Embedded PostgreSQL as a library — real Postgres, no server process"
